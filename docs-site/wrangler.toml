# Cloudflare Workers + Static Assets configuration for the promptregistry docs site.
# Deployed via `wrangler deploy` after `npm run build` produces ./build.

name = "promptregistry"
compatibility_date = "2026-05-04"

[assets]
directory = "./build"
not_found_handling = "404-page"
